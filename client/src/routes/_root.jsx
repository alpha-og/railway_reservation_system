/*import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_root')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/_root"!</div>
}*/
/*
import { createRootRoute, Outlet, createFileRoute } from '@tanstack/react-router';
import React from 'react';
import Navbar from '../../components/nav.jsx'; // Assuming you have a Navbar component
import Footer from '../../components/Footer'; // Assuming you have a Footer component

export const Route = createRootRoute('/_root')({
  component: () => (
    <>
      <Navbar />
      <hr />
      <Outlet />
      <hr />
      <Footer />
    </>
  ),
});*/
